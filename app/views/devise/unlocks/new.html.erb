
<div class="container-sm py-5">
  <div class="row card bg-color3 p-3">
    <h1 class="text-center"><%= t('.resend_unlock_instructions') %></h1>
    <%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "col-6 col-md-8 mx-auto" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-inputs">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
      </div>

      <div class="form-actions">
        <%= f.submit t('.resend_unlock_instructions'), class: "btn btn-secondary" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>

  </div>
</div>
