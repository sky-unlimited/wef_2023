<div class="container-md">
  <h2><%= t('subscribers.unsubscribe.title') %></h2>
  <p><b><%= @email %></b> <%= t('subscribers.unsubscribe.email_unsubscribe') %></p>
  <div class="mt-20">
    <%= link_to 'Back', root_path, class: "btn btn-secondary" %>
  </div>
</div>
