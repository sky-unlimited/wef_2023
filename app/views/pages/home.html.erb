<%#= render 'seo' %>
<div class="home-banner">
  <div class="container">
    <div class="home-banner__body container-lg">
      <h1><%= t('pages.home.hero_one') %></h1>
      <p><%= t('pages.home.hero_two') %></p>
      <p><%= link_to t('pages.home.banner-button'), new_trip_request_path, class: "btn btn-primary" %></p>
    </div>
  </div>
</div>
<div class="container">
</div>
